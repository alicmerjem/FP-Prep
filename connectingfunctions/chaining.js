// CHAINING
// calling one method after another on the same object


// example 1
[1, 2, 3]
    .map(x => x * 2)
    .filter(x => x > 2);


// another example cause yes
"   HELLO WORLD!    "
    .trim()
    .toLowerCase()
    .replace("!", ".");